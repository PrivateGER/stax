# Stax

A Plex/Jellyfin replacement with actually good synced play.

This project is 100% LLM slop and should not be exposed publicly or used for anything important.

If you run it anyway:

```bash
./syncplay-backend --api-addr 0.0.0.0:3001 --hw-accel nvenc --library-root /your-files/Anime/ --database-path /tmp/stax.db --thumbnail-dir /tmp/stax-thumbnails
```
